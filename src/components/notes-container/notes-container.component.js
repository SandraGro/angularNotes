import template from './notes-container.tpl.html';
import controller from './notes-container.controller';

export const NotesContainer = {
    template,
    replace: true,
    controller,
    controllerAs: 'vm',
    bindings: {
        
    }
};
